* move all modules to V0::...
* placeholders D::C::{Array,Dictionary} for people to isa() for
* V1 collections that have a pointer table type (with size) and arrays/dicts just have a pointer to it;
** de-dup identical pointer tables
