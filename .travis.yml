language: perl
matrix:
  include:
    - perl: "5.20"
      dist: trusty
      before_install:
        - cpanm --quiet --notest Devel::Cover::Report::Coveralls
      script: "perl Makefile.PL && make && cover -ignore blib/lib/Data/CROD/Array.pm -ignore blib/lib/Data/CROD/Dictionary.pm -test -report coveralls"
sudo: false
