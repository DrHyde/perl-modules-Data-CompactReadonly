package Data::CROD::Dictionary::Long;

use warnings;
use strict;
use base 'Data::CROD::Dictionary';

use Data::CROD::Scalar::Long;

# this class only exists so it can encode the length's
# type in its name, and load that type

1;
