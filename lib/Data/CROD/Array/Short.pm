package Data::CROD::Array::Short;

use warnings;
use strict;
use base 'Data::CROD::Array';

use Data::CROD::Scalar::Short;

# this class only exists so it can encode the length's
# type in its name, and load that type

1;
