package Data::CROD::Text::Medium;

use warnings;
use strict;
use base 'Data::CROD::Text';

use Data::CROD::Scalar::Medium;

# this class only exists so it can encode the length's
# type in its name, and load that type

1;
