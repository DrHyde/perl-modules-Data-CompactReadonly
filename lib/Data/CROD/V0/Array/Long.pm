package Data::CROD::V0::Array::Long;

use warnings;
use strict;
use base 'Data::CROD::V0::Array';

use Data::CROD::V0::Scalar::Long;

# this class only exists so it can encode the length's
# type in its name, and load that type

1;
