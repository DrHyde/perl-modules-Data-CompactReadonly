package Data::CROD::V0::Dictionary::Medium;

use warnings;
use strict;
use base 'Data::CROD::V0::Dictionary';

use Data::CROD::V0::Scalar::Medium;

# this class only exists so it can encode the length's
# type in its name, and load that type

1;
